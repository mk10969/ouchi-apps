apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  # - upstream/tekton-dashboard-readonly.yaml
  - upstream/tekton-dashboard.yaml

# fix: SharedResourceWarning
patches:
  - target:
      kind: CustomResourceDefinition
    patch: |
      $patch: delete
      kind: CustomResourceDefinition
      metadata:
        name: extensions.dashboard.tekton.dev
